<!DOCTYPE html>
<html>
<head>
	<title>BSocial</title>
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'vendor/modernizr' %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% flash.each do |type, message| %>
	<div class="flash alert-box text-center <%= type %>">
		<%= message %>
		<a href="#" class="close">&times;</a>
	</div>	
<% end %>

<header class="row global-header">
	<div class="small-12 small-centered medium-8 medium-centered large-5 large-centered column">
		<%= link_to "Post List", posts_path %>
		<% if logged_in? %>
			<%= link_to "Log Out", logout_path, method: :delete %>
		<% else %>
			<%= link_to "Log In", login_path %>
		<%= link_to "Sign Up", new_user_path %>
		<% end %>
	</div>
</header>


<div class="row collapse">
	<div class="small-12 small-centered medium-8 medium-centered large-5 large-centered column">
		<%= yield %>
	</div>
</div>

<footer class="row">
	<div class="small-12 small-centered medium-8 medium-centered large-5 large-centered column text-centered">
		<% if logged_in? %>
			<%= link_to "Seetings", edit_user_path(current_user) %>
		<% end %>
	</div>
</footer>
</body>
</html>
